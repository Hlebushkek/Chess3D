using System.Collections;
using System.Collections.Generic;
using Photon.Pun;
using UnityEngine;

[RequireComponent(typeof(PhotonView))]
public class EndRoundManager : MonoBehaviour
{
    private PhotonView photonView;
    [PunRPC]
    private void Start()
    {
        
    }
    public void ShowEndMenuRPC()
    {
        
    }
}
